import { DebtOrder } from "../types";
export declare abstract class BaseAdapter {
    abstract generateAsync(params: object): Promise<DebtOrder.Instance>;
    abstract validateAsync(debtOrder: DebtOrder.Instance): Promise<void>;
}
